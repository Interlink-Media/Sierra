package de.feelix.events.listeners;

/**
 * The Listener interface is used to mark classes as event listeners.
 * Event listeners are classes that contain methods annotated with the {@link Eventable} annotation.
 * These methods will be called when specific events are dispatched.
 */
public interface EventListener {
}
